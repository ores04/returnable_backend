# FastAPI Server Configuration
PROJECT_NAME="Returnable FastAPI Server"
VERSION="1.0.0"
DESCRIPTION="A FastAPI server with blueprint-like structure"

# Server Settings
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS Settings
#BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8000
